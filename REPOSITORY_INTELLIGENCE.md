<!--
Generated by Laguna M.1 from codebase read — 
runtime claims marked UNVERIFIED require device confirmation.
-->

# WardReady — Repository Intelligence

## 1. Hidden Architecture

### Implicit Feature Relationships
- **Auth → Subscription Chain**: `isSubscribedProvider` (subscription_repository.dart) calls Supabase without network guard; failure falls through to grace period logic
- **Quiz → Notification Chain**: `SpacedRepetitionService.recordReview()` → `NotificationService.scheduleDueReminder()` — review recording directly triggers notification scheduling
- **WeaknessService Dependency**: `ArticleDetailScreen` reads `weakFieldsProvider` which depends on `_ensureLastQualityColumn()` — article viewing triggers schema migration

### Hidden Dependencies
- `QuizNotifier` depends on `QuizRepository`, `QuizSyncService`, `SpacedRepetitionService` all simultaneously — violates single responsibility
- `StreakNotifier` and `ProgressNotifier` both query `study_sessions` table independently — potential for inconsistent reads
- `CategoriesScreen.initState` (categories_screen.dart:120-143) invalidates ALL category progress providers after sync — fires 25+ provider invalidations

### Provider Chains
- `allArticlesProvider` (article_repository.dart) → StreamProvider → watches DB changes
- `weakFieldsProvider` (weakness_service.dart) → FutureProvider.family → triggers `_ensureLastQualityColumn()` on first read
- `examSessionProvider` (exam_session_notifier.dart) → StateNotifier → holds 200 questions in memory

### Screens Depending On Implementation Details
- `ArticleDetailScreen` (article_detail_screen.dart:122) expects `ArticleLocal` to have exactly 10 content fields — crash if structure changes
- `QuizScreen` (quiz_screen.dart) hardcodes `AppConfig.internalMedicineCategory` as default — cannot change category without code change
- `SettingsScreen` (settings_screen.dart:32) reads `MigrationErrorStore.value` from app_database.dart — UI couples to migration state

### Services Acting As Coordinators
- `NotificationService` (notification_service.dart) — coordinates DB queries for due count + notification scheduling
- `ArticleRepository` (article_repository.dart) — coordinates network sync + local DB writes + state notifications
- `SyncStateNotifier` (sync_state_provider.dart) — central coordinator for sync status across all repositories

---

## 2. Coupling Analysis

### Very High
- **main.dart** — Changes to routing affect all navigation; changes to providers affect app bootstrap
- **app_database.dart** — Schema changes require `build_runner`; all repositories depend on it

### High
- **quiz_notifier.dart** — Coupled to QuizRepository, QuizSyncService, SpacedRepetitionService; breaking change affects entire quiz flow
- **article_repository.dart** — Powers all article list/search/bookmark flows; coupling to sync state providers
- **categories_screen.dart** — Invalidates 25+ providers on sync completion; tight coupling to AppConfig categories

### Medium
- **spaced_repetition_service.dart** — Used by QuizNotifier and NotificationService; breaking changes affect quiz + notifications
- **weakness_service.dart** — Used by ArticleDetailScreen; changes affect article rendering
- **article_detail_screen.dart** — Direct coupling to DB schema via `weakFieldsProvider`

### Low
- **empty_state.dart** — Only UI widget, no logic coupling
- **error_banners.dart** — Function defined but never used
- **terms_content.dart** — Static content, no behavioral coupling

---

## 3. Single Points of Failure

- **main.dart** — App entry point; crash here kills app; auth state logic drives all navigation
- **ArticleRepository** — Single point for all article data; if sync fails, entire library empty
- **AppDatabase** — All local storage; database corruption = data loss
- **QuizNotifier** — Single point for quiz state; breaking changes affect all quiz sessions
- **spaced_repetition_service.dart** — SM-2 algorithm is locked but incorrect logic = wrong review scheduling
- **subscription_repository.dart** — Single point for access control; network failure = potential lockout
- **connectivity_notifier.dart** — Drives offline banners; incorrect state = false positives

---

## 4. Dead Code

### Unused Imports
- `lib/features/articles/data/article_search_provider.dart:8` — `package:sqlite3/sqlite3.dart` (ignore comment present)
- `lib/features/quiz/quiz_repository.dart:8` — `package:sqlite3/sqlite3.dart` (ignore comment present)

### Unused Classes
- `lib/features/articles/models/article_model.dart:1` — Entire `ArticleContent` class never instantiated

### Unused Methods
- `lib/core/widgets/error_banners.dart:3` — `showDiskFullBanner` never invoked
- `lib/features/auth/data/auth_service.dart:129-139` — `initialize()` method never called in main.dart

### Unused Constants
- `lib/features/articles/presentation/article_list_screen.dart:21` — `subcategoriesByCategory` only partially used (only Internal Medicine has subcategories defined)

### Obsolete
- `lib/features/quiz/data/quiz_sync_service.dart:1` — Re-export file, actual implementation in `lib/features/quiz/quiz_sync_service.dart`

---

## 5. Duplicate Logic

### Error Handling Patterns
- **admin_repository.dart:64-79** and **quiz_repository.dart:57-73** — Identical 401/403/429/503/504 handling with `postgrestStatus()` check

### Shimmer Loading Patterns
- **categories_screen.dart:245-264** and **progress_screen.dart:213-226** — Identical container dimensions and color scheme

### SQL Queries
- **app_database.dart:186-222** (`_ensureStudySessionsTable`) and **weakness_service.dart:38-47** (`_ensureLastQualityColumn`) — Identical column existence check + ALTER pattern

### Validation
- **login_screen.dart:96-105** and **signup_screen.dart:117-126** — Email validation with `trim()` + `@` check
- **login_screen.dart:119-124** and **signup_screen.dart:140-145** — Password empty check with `trim().isEmpty`

### Query Logic
- **article_repository.dart:128-150** (`watchLocalArticles`) and **article_repository.dart:152-178** (`watchArticlesPaged`) — 90% identical filtering logic

---

## 6. State Management Review

### Providers That Should Be autoDispose
- `dailyStudyRemindersEnabledProvider` — User preference, should survive but OK as-is
- `weakFieldsProvider` — Cache could grow unbounded; should be autoDispose

### Providers With Excessive Rebuilds
- `allArticlesProvider` — StreamProvider rebuilds on ANY article table change (including bookmarks)

### Provider Ownership Issues
- `articleSearchControllerProvider` — Correctly autoDispose
- `quizNotifierProvider` — Family provider correctly keyed by category

### StateNotifier Candidates
- `ArticleListController` already uses StateNotifier — appropriate
- `QuizNotifier` — Already StateNotifier — appropriate

### Providers Doing Repository Work
- `articleSearchControllerProvider` — Controller calls repository methods directly — acceptable pattern

---

## 7. Database Review

### Expensive Queries
- **article_search_provider.dart:189-200** (`_ensureSearchIndex`) — Full table scan + loop insert for FTS5 index rebuild
- **exam_session_notifier.dart:128-166** (`_selectWeighted200Questions`) — Multiple category queries in loop

### Missing Indexes
- `article_search_fts` — FTS5 virtual table exists but no explicit index on `article_id` for joins

### Redundant Conversions
- **app_database.dart:218-222** — `read<String>('date')` then string manipulation when DateTime would work

### Large Table Scans
- **article_search_provider.dart:272-298** — `_ensureSearchIndex` scans ALL articles for FTS5 rebuild

### Inefficient Pagination
- **article_repository.dart:207-231** — Opens new query for each page; no cursor optimization

---

## 8. Performance Hotspots

### Expensive Rebuilds
- **categories_screen.dart:120-143** — Sync completion invalidates ALL category progress providers

### Large Widget Trees
- **article_detail_screen.dart:429-479** — Builds all section widgets in list, even when collapsed

### Synchronous Work
- **search_history_service.dart:19** — String list deduplication in saveSearch executed synchronously

### Database Loops
- **article_search_provider.dart:279-298** — Loop over all articles for FTS5 indexing

### Search Bottlenecks
- **article_search_provider.dart:191-201** — Falls back to full table scan if FTS5 corrupted

### Memory Pressure
- **exam_session_notifier.dart:205-207** — Loads up to 200 questions in memory simultaneously

---

## 9. Security Review

### Authentication
- **Supabase Auth** — Uses `signInWithPassword` and `signUp` correctly
- **Token Storage** — Uses `flutter_secure_storage` for access/refresh tokens (auth_service.dart:110-113)

### Authorization
- **Admin Check** — `currentAdminProfileProvider` queries `is_admin` flag; 403 handled but silent fail
- **Subscription Guard** — GoRouter redirect (main.dart:151-159) checks `isSubscribedProvider`

### Supabase Usage
- **select()** calls use specific columns — good
- **PostgrestException** handling present — good

### Session Handling
- **Session Timeout** — 30-minute idle logout (session_timeout_provider.dart)
- **Grace Period** — 30-day offline grace in subscription check

### Environment Variables
- **env.dart** — Uses `String.fromEnvironment` with empty defaults — credentials must be passed at build time

### RLS Assumptions
- **RLS Rejection Handling** — 403 triggers debugPrint + fallback to cache (quiz_repository.dart, admin_repository.dart) — but user sees stale data silently

---

## 10. Scalability

### Well-Scaled
- **Riverpod async providers** — Handle loading states gracefully
- **Pagination** — Article list uses pageSize of 20
- **FTS5** — Full-text search scales well

### Potential Failures
- **exam_session_notifier.dart:93-102** — `_domainCount` fires N sequential queries for category weights — 250k questions × 17 categories = 17 queries
- **weakness_service.dart:12-35** — Query for weak fields could be slow on large quiz_table
- **spaced_repetition_service.dart:41-65** — Query for due cards scans entire table — needs index on `next_due_at`
- **subscription_repository.dart:18-63** — Serial fetch of all users for admin dashboard won't scale to 100k

---

## 11. Maintainability Score

| Subsystem | Score | Rationale |
|-----------|-------|-----------|
| Architecture | 8/10 | Clean feature separation, consistent patterns |
| Database | 6/10 | Drift migrations are thorough but schema has legacy columns (`session_date`) |
| Repositories | 7/10 | Good error handling, but duplication and coupling issues |
| Providers | 7/10 | Mostly correct Riverpod usage, some could be autoDispose |
| Theme | 9/10 | Centralized, Material 3, consistent colors |
| Authentication | 7/10 | Secure token handling but silent failures on 403 |
| Quiz | 6/10 | SM-2 locked but overly coupled to repository layer |
| Sync | 7/10 | Good offline handling but no retry/backoff |
| Search | 6/10 | FTS5 good but fallback is full scan |
| Navigation | 8/10 | GoRouter clean, redirect guards present |

---

## 12. Technical Debt Ranking

### Critical
- None identified

### High
- **QuizNotifier coupling** — Tight to 3 services; refactoring unlocks maintainability
- **Duplicate error patterns** — Miss maintenance opportunities

### Medium
- **Unused `fsrs` package** — Could confuse future developers
- **Database recovery `exit(0)`** — Abrupt termination
- **`weakFieldsProvider` not autoDispose** — Potential memory growth

### Low
- **Duplicate shimmer widgets** — Acceptable duplication
- **`ArticleContent` unused** — Dead code removal
- **`showDiskFullBanner` unused** — Dead code removal

---

## 13. Refactoring Opportunities

### Top 25 Refactorings

1. **Extract error handling to mixin** — Consolidate 401/403/429/503/504 pattern across repositories — Effort: 2h, Risk: Low, Payoff: High

2. **Make weakFieldsProvider autoDispose** — Prevent unbounded growth — Effort: 30m, Risk: Low, Payoff: Medium

3. **Remove unused ArticleContent class** — Clean dead code — Effort: 15m, Risk: None, Payoff: Low

4. **Remove unused showDiskFullBanner** — Clean dead code — Effort: 15m, Risk: None, Payoff: Low

5. **Add index to quiz_table on next_due_at** — Performance gain for SM-2 queries — Effort: 30m, Risk: Low, Payoff: High

6. **Extract shimmer widgets to shared file** — Reduce duplication — Effort: 45m, Risk: Low, Payoff: Low

7. **Refactor QuizNotifier to separate concerns** — Extract sync logic — Effort: 4h, Risk: High, Payoff: High

8. **Add retry/backoff to sync operations** — Better offline resilience — Effort: 3h, Risk: Medium, Payoff: High

9. **Optimize exam question selection to single query** — Replace N queries — Effort: 2h, Risk: Medium, Payoff: High (scale)

10. **Remove fsrs package from pubspec.yaml** — Clean unused dep — Effort: 15m, Risk: None, Payoff: Low

11. **Remove google_fonts package from pubspec.yaml** — Clean unused dep — Effort: 15m, Risk: None, Payoff: Low

12. **Call auth_service.initialize() in main.dart** — Ensure session restore — Effort: 30m, Risk: Low, Payoff: Medium

13. **Add notification permission flow** — Android 13+ compliance — Effort: 2h, Risk: Medium, Payoff: Medium

14. **Cache category progress queries** — Reduce DB load on sync — Effort: 1h, Risk: Low, Payoff: Medium

15. **Add FTS5 trigger for auto-indexing** — Eliminate rebuild loop — Effort: 2h, Risk: Medium, Payoff: High

16. **Extract ArticleListState to separate file** — Co-location — Effort: 45m, Risk: Low, Payoff: Low

17. **Add context.mounted check in article detail bookmark StreamBuilder** — Missing guard — Effort: 15m, Risk: Low, Payoff: Low

18. **Replace exit(0) with proper app restart** — Better UX — Effort: 30m, Risk: Low, Payoff: Low

19. **Unify search_history_provider naming** — Two search screens exist — Effort: 1h, Risk: Low, Payoff: Low

20. **Add exam route and screen** — Complete EHPLE Exam Mode — Effort: 8h, Risk: High, Payoff: High (feature completeness)

21. **Add FLAG_SECURE to AndroidManifest.xml** — Security hardening — Effort: 15m, Risk: Low, Payoff: Low

22. **Optimize pagination to use cursor** — Better performance — Effort: 2h, Risk: Medium, Payoff: Medium

23. **Add unit tests for sync state handling** — Quality — Effort: 2h, Risk: Low, Payoff: Medium

24. **Add integration tests for quiz flow** — Quality — Effort: 4h, Risk: Low, Payoff: Medium

25. **Add proper logging infrastructure** — Replace debugPrint — Effort: 3h, Risk: Medium, Payoff: Medium

---

## 14. Roadmap (Next 6 Months)

### Month 1: Stabilization
- Remove dead code (ArticleContent, showDiskFullBanner, unused packages)
- Fix QuizNotifier coupling (extract sync, simplify)
- Add proper session restore call in main()

### Month 2: Performance
- Add missing database indexes (next_due_at, last_quality)
- Optimize exam question selection
- Add FTS5 trigger for auto-indexing

### Month 3: Completeness
- Complete EHPLE Exam Mode UI (ExamScreen, ExamResultsScreen)
- Add FLAG_SECURE to AndroidManifest
- Add notification permission flow

### Month 4: Resilience
- Add retry/backoff to all network operations
- Improve sync status visibility
- Add proper logging infrastructure

### Month 5: Testing
- Expand unit test coverage
- Add integration tests for critical flows
- Test with large datasets (100k questions)

### Month 6: Release Prep
- Security audit (token handling, RLS)
- Performance validation on target devices
- Documentation finalization