<!--
Generated by Laguna M.1 from codebase read — 
runtime claims marked UNVERIFIED require device confirmation.
-->

# WardReady — Release Checklist

## Must Fix Before Closed Beta

- [ ] **Supabase key validation** — Add assertion in main() that keys are non-empty in release mode  
  File: `lib/app/env.dart:2-8`, `lib/main.dart:81-84` | Effort: 30m | Risk: App silently non-functional

- [ ] **Notification runtime permission** — Handle Android 13+ permission before enabling reminders  
  File: `lib/core/services/notification_service.dart:115`, `lib/features/settings/settings_screen.dart:76-84` | Effort: 2h | Risk: Notifications silently fail

- [ ] **Session restore initialization** — Call `authServiceProvider.initialize()` in main()  
  File: `lib/features/auth/data/auth_service.dart:129-139` | Effort: 15m | Risk: Users re-login on every restart

- [ ] **FLAG_SECURE** — Prevent screenshots in medical app  
  File: `android/app/src/main/AndroidManifest.xml` | Effort: 15m | Risk: Policy violation

## Must Fix Before Production

- [ ] **EHPLE Exam Mode UI** — Add missing ExamScreen and ExamResultsScreen  
  File: `lib/features/quiz/exam_session_notifier.dart` needs route | Effort: 8h | Risk: Feature incomplete

- [ ] **Admin list pagination** — Add limit/cursor to `fetchAllUsers()`  
  File: `lib/features/admin/data/admin_repository.dart:54-64` | Effort: 4h | Risk: OOM at scale

- [ ] **QuizNotifier decoupling** — Extract tri-service dependency into orchestrator  
  File: `lib/features/quiz/quiz_notifier.dart:17-34` | Effort: 4h | Risk: Refactor fragile code

- [ ] **Exam question selection performance** — Replace 17 N+1 queries with single query  
  File: `lib/features/quiz/exam_session_notifier.dart:128-207` | Effort: 3h | Risk: Slow exam start

- [ ] **Progress screen tab** — Add 5th tab to MainShell for heatmap  
  File: `lib/app/main_shell.dart` | Effort: 4h | Risk: Feature incomplete

- [ ] **key.properties template** — Create signing configuration for CI  
  File: No key.properties in repo | Effort: 30m | Risk: Cannot sign release builds

## Nice to Have (Beta Feedback)

- [ ] **Unused packages** — Remove `fsrs` and `google_fonts` from pubspec.yaml  
  File: `pubspec.yaml:18,20` | Effort: 15m | Risk: APK bloat

- [ ] **Database recovery UX** — Replace `exit(0)` with graceful restart  
  File: `lib/core/screens/database_recovery_screen.dart:63` | Effort: 30m | Risk: Poor UX

- [ ] **Provider invalidation storm** — Consolidate category progress queries  
  File: `lib/features/home/presentation/categories_screen.dart:120-143` | Effort: 2h | Risk: UI jank

- [ ] **Accessibility labels** — Add semantics to interactive widgets  
  Effort: 4h | Risk: TalkBack failures

- [ ] **Crash reporting** — Add Sentry or Crashlytics  
  Effort: 2h | Risk: Cannot diagnose production issues

## Can Wait Until v1.1

- [ ] Password recovery flow via Supabase
- [ ] Search history encryption
- [ ] Light theme testing
- [ ] Deep linking for articles
- [ ] Analytics tracking
- [ ] Tablet/responsive layouts
- [ ] In-app review prompt

## Configuration Checklist

- [ ] Verify versionCode increments (currently 1)
- [ ] Verify versionName matches release (currently "1.0.0")
- [ ] Test release build locally with signing config
- [ ] Generate Play Store assets (screenshots, feature graphic, icon)
- [ ] Privacy policy URL ready
- [ ] Terms of Service URL ready
- [ ] Data safety form in Play Console completed

## Signing Checklist

- [ ] Create `key.properties` with valid keystore path
- [ ] Generate `app-release-key.jks` (or use existing)
- [ ] Secure passwords (not in source control)
- [ ] Test `flutter build apk --release` succeeds