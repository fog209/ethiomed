<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Certificate pinning is intentionally disabled pending public launch per owner decision. -->
  <!--
    NOTE: Placeholder pins were removed to restore connectivity.
    To enable pinning in the future:
      1) Extract SHA-256 SPKI pin:
         openssl s_client -connect kxcdzlyirdonkipcymvc.supabase.co:443 </dev/null 2>/dev/null | openssl x509 -pubkey -noout | openssl dgst -sha256 -binary | base64
      2) Add <pin-set> with real pin value
  -->
  <domain-config cleartextTrafficPermitted="false">
    <domain includeSubdomains="true">kxcdzlyirdonkipcymvc.supabase.co</domain>

    <!-- Certificate pinning disabled until real SHA-256 pins are configured -->
    <!-- See: openssl command above -->
  </domain-config>

  <!-- Allow system default trust for any other domains not listed above -->
</network-security-config>
