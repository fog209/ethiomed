<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!--
    Certificate pinning scaffold for Supabase domain.
    TODO: Replace the placeholder pin values with the real SHA-256 pins.

    How to extract pins (for each certificate in the chain as needed):
      1) Determine the host:
         kxcdzlyirdonkipcymvc.supabase.co
      2) Fetch the leaf certificate(s) and compute SHA-256 SPKI pins.
         Example commands (run locally):
           - openssl s_client -connect kxcdzlyirdonkipcymvc.supabase.co:443 -servername kxcdzlyirdonkipcymvc.supabase.co </dev/null 2>/dev/null | openssl x509 -pubkey -noout | openssl dgst -sha256 -binary | base64
      3) Paste the resulting base64 into the pins below (sha256="...").

    Notes:
    - Do not fabricate pin hashes. Keep TODO markers until you insert real values.
  -->
  <domain-config cleartextTrafficPermitted="false">
    <domain includeSubdomains="true">kxcdzlyirdonkipcymvc.supabase.co</domain>

    <pin-set
      expiration="2028-01-01">
      <!-- TODO: Replace with real SHA-256 pin for the Supabase leaf certificate -->
      <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>

      <!-- TODO: Optionally pin the next/alternate certificate to support rotation -->
      <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
    </pin-set>
  </domain-config>

  <!-- Allow system default trust for any other domains not listed above -->
</network-security-config>
